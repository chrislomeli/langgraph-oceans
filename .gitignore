# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Envs
.venv/
venv/
.env
.env.*

# OS / IDE
.DS_Store
.idea/

# Data / artifacts (large; keep out of git). Image blobs now live OUTSIDE the
# repo (see Settings.image_root), so we no longer ignore all of /src/data/ —
# that rule was hiding the pipeline source. Only the data files are kept out.
*.parquet
*.csv
catalog_images/
