"""models — Layer 5: the model layer (embedders, LLM roles).

Holds the *learned* components the tools depend on. `image_embedder` is the CLIP
fluke embedder, the single source of truth for how an image becomes a vector.
"""
