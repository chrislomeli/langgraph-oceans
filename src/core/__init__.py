"""core — the platform substrate: domain-agnostic infrastructure (config,
LLM registry, prompts, exceptions, logging, and the generic agent-graph
framework in core/agents). Imports nothing domain-specific; the ocean domain
(agents/, tools/, models/, stores/) depends on core, never the reverse."""
